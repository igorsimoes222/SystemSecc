[build]
  functions = ".netlify/blobs-serve/functions"
  publish = "Public"

[dev]
  command = "npm start"          # Comando para rodar seu servidor local (mude para o comando que você usa)
  targetPort = 3000              # Porta do servidor local

[build]
  command = "npm run build"
  publish = "Public"


[functions]
  directory = "netlify/functions"

[context.production.environment]
  NODE_ENV = "production"